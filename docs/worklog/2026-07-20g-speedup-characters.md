# 2026-07-20(夜の部): レイテンシ改善・2キャラ切替・フィラー音声

## やったこと

1. **往復20秒問題の解明と修正**: Worker内部7.8秒 vs 体感20秒の差分からアップロード遅延を特定 → TCP送信バッファ64KB化(troubleshooting.md参照)
2. **TLS接続の使い回し**: talk間でEspHttpConnectionを保持(ハンドシェイク約1.5秒を初回のみに)
3. **モデルA/B**: STTを`gpt-4o-mini-transcribe`へ(採用)。TTSの`gpt-4o-mini-tts`は日本語音質悪化で`tts-1`に戻した
4. **文分割ストリーミングTTS**: 実装→発音が不自然で撤回 → **フィラー音声先行方式**に置き換え(decisions.md参照)
5. **2キャラ目(アニメ調)の追加**: 320×155仕様準拠を確認、ベゼル基準の位置合わせ不可のため無補正変換。`tools/face_convert.py`をキャラ別ディレクトリ対応に拡張
6. **キャラ切替UI**: ダブルタップ実装→PTTと衝突し廃止→**[顔]ボタン**方式へ。チャイム(2音+末尾無音)+NVS永続化
7. 顔アニメーションと音声再生の両立(SPIバッファ12KB+I2S DMA500ms+再生ループ内アニメ)
8. UX調整: ビープ音量8000→1250、成功時のデバッグ表示を画面から撤去、ステータス文字の重なり修正、応答再生中の割り込み(バージイン)

## 計測値

- TCPバッファ修正前: 155KBアップロードに約7秒(RTT200ms×5.7KB窓)
- Worker内部(高速STT後): stt≒1.5〜2.3s / llm≒1〜2s / tts≒2〜8s(応答長に比例)
- 外れ値: ネットワーク停滞で52秒の往復が1回発生(中継器経由のRTT変動94〜278ms)

## 未解決・次回

- キャラ2のフレーム間ガタつき(位置合わせなし)の実機評価
- 実機での「体感待ち時間」再計測(フィラー方式の効果確認)
- バッテリー運用の持ち時間実測(500mAh、目安1時間前後)
