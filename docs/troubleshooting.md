# ハマりどころ記録

遭遇したエラー・ハマりどころ・回避策を時系列で記録する。本の「ハマりどころコラム」の一次素材。エラーメッセージは原文のまま残すこと。

## 2026-07-14: `.zshrc` への追記が既存行と連結して壊れた

環境変数の永続化のため `.zshrc` に `source $HOME/export-esp.sh` を追記した際、既存の最終行に改行が無かったため、追記内容が前の行と連結されて `.zshrc` が壊れた。

- **症状**: 新しいシェルで環境変数が読み込まれない(壊れた行がエラーになる)
- **回避策**: `.zshrc` を開いて手動で2行に分離
- **教訓**: `echo >> ~/.zshrc` で追記する前に、ファイル末尾に改行があるか確認する(`tail -c 1 ~/.zshrc | xxd` で確認できる)

## 2026-07-19: `cargo run` に espflash のオプションを渡すには `--` が必要

シリアルポートを明示指定しようと `cargo run --port /dev/cu.usbmodem1101` と実行したら cargo に拒否された。

```
error: unexpected argument '--port' found

  tip: to pass '--port' as a value, use '-- --port'
```

- **原因**: `--port` は cargo のオプションではなく、runner(espflash)のオプション。cargo に渡す引数と runner に渡す引数は `--` で区切る
- **回避策**: `cargo run -- --port /dev/cu.usbmodem1101` とする。ポートが1つしか無い場合は espflash が自動検出するので省略も可

## 2026-07-19: esp-idf-hal 0.46 で `prelude` モジュールが廃止されている

ネット上のサンプルコードによくある `use esp_idf_hal::prelude::*;` が 0.46.2(git版)ではコンパイルエラーになる。

```
error[E0432]: unresolved import `esp_idf_hal::prelude`
  --> src/main.rs:11:5
   |
11 |     prelude::*,
   |     ^^^^^^^ could not find `prelude` in `esp_idf_hal`

error[E0689]: can't call method `kHz` on ambiguous numeric type `{integer}`
```

- **原因**: esp-idf-hal の破壊的変更で prelude が削除された。`.kHz()` / `.MHz()` などの単位変換メソッドは prelude 経由で入っていた
- **回避策**: `use esp_idf_hal::units::FromValueType;` を明示的にインポートする
- **教訓**: esp-idf-hal 系は破壊的変更が多い。ネットのサンプルはバージョン確認必須

## 2026-07-20: espflash monitor の出力はパイプ経由だとバッファリングされて届かない

`cargo run ... | grep ...` のようにシリアルモニタの出力をパイプで加工すると、espflash側の標準出力がブロックバッファリング(約8KB単位)になり、アプリのログがいつまでもパイプ先に流れてこない。前回はブートログが途中で切れた状態のまま止まって見えた。

- **症状**: 実機は正常動作しているのに、パイプ先のログファイルが空 or ブートログ途中で止まる
- **原因**: 標準出力がTTYでない場合のlibcのバッファリング挙動
- **回避策**: ログをファイルに落として加工したい場合は `script -q /dev/null cargo run ...` のように疑似TTYを噛ませる。目視デバッグならターミナルで直接 `cargo run` すれば問題ない

## 2026-07-20: Wi-Fi接続が「認証OK→アソシエーションで切断」を繰り返す

親機のSSIDを指定したところ、以下のパターンで接続に失敗し続けた。

```
I (3875) wifi:state: init -> auth (0xb0)
I (3885) wifi:state: auth -> assoc (0x0)
I (4885) wifi:state: assoc -> init (0x400)
Wi-Fi接続に失敗(SSID/パスワードを確認): ESP_ERR_TIMEOUT (error code 263)
```

- **症状**: 電波は見えていて認証(auth)まで通るが、アソシエーション段階で切断されタイムアウト
- **解決**: 接続先を中継器のSSID(`..._EXT`)に変更したら一発で接続できた。台所のデバイス設置場所からは親機が遠く、中継器経由が正解だった
- **教訓その1**: 「パスワード間違い」なら4-wayハンドシェイク段階で失敗する。アソシエーションで落ちる場合は電波状況・ルーター側の事情を疑う
- **教訓その2**: この過程で接続リトライループ(最大5回)を実装した。ESP32のWi-Fi初回接続は一発で通らないことがあるので、リトライは必須の設計

## 2026-07-20: シリアルログのバッファリングは `script` コマンドでも解決せず → 画面表示が最強

espflash monitor の出力を疑似TTY(`script -q /dev/null`)経由でファイルに落としても、やはり途中でバッファに滞留して読めなくなる現象が再発。最終的にWi-Fi接続結果は「LCDにIPアドレスを描画する」ことで確認した。

- **教訓**: 組込み開発では「デバイス自身の画面にステータスを出す」のが最も確実なデバッグ手段。シリアルはあくまで補助と考える(本番の台所運用ではUSBを繋がない前提でもあり、画面表示ファーストの設計は今後も踏襲する)

## 2026-07-20: ビープ音が止まらない — I2S送信DMAは最後のバッファを繰り返す

150msのビープを再生したところ、鳴りっぱなしになった。

- **原因**: ESP-IDFのI2S送信は、書き込むデータが尽きるとDMAが**最後のバッファ内容を繰り返し出力し続ける**。サイン波で終わるデータを書くと、その末尾が無限ループで鳴り続ける
- **回避策**: 再生データの**末尾に十分な長さの無音(今回300ms)を付ける**。DMAが繰り返すのは無音になるので実害がなくなる
- **緊急停止**: 実機が鳴りっぱなしになったときは `espflash reset --port /dev/cu.usbmodemXXX` でMacから即座にリセットできる(電源ボタン4秒長押しより速い)。**音が出るコードを書くときは、この停止手段を先に確認しておくこと**

## 2026-07-20: ビープ再生中に音量メーターが止まる — ブロッキング書き込みの罠

タッチ→ビープ再生の間(450ms)、画面のレベルメーターが凍った。

- **原因**: 1つのループで「録音読み→描画→タッチ→再生」を直列処理しており、`i2s.write(..., BLOCK)` が全データを書き終わるまでループ全体が止まるため
- **回避策**: 再生位置カーソル(`Option<usize>`)を持ち、ループ1周ごとに**タイムアウト0で書ける分だけ書く**非ブロッキング方式に変更。ループは録音読み(32ms周期)でペーシングされているので、毎周の書き込みで途切れず再生される
- **教訓**: 将来のTTS音声再生(数秒〜数十秒)では、この方式かスレッド化(ESP32-S3はデュアルコア、std::threadが使える)が必須になる

## 2026-07-20: 録音後にデバイスが黙って再起動 — 96KBのメモリ確保失敗

タッチ→3秒録音までは動くのに、送信フェーズに入ると画面が起動シーケンス(WiFi connecting→IP)に戻る。シリアルを繋いで正体を確認:

```
memory allocation of 96044 bytes failed
abort() was called at PC 0x4203cabe on core 0
```

- **症状**: 画面上はエラーも出ず再起動ループに見える(パニック→abort→リブートのため)
- **原因**: 録音バッファ96KBを保持したまま、WAVヘッダ結合用にもう96KBを確保する設計だった。内蔵RAM約330KBからWi-Fiスタック等を引いた残りでは連続96KB×2は確保できない
- **対策1**: WAVヘッダ(44バイト)と本体を別々にHTTP書き込みし、結合バッファを作らない
- **対策2**: PSRAM 8MBを有効化(下記エントリ参照)
- **教訓**: 「画面が勝手に起動画面に戻る」はパニック再起動を疑い、まずシリアルでパニックメッセージを取る

## 2026-07-20: PSRAM有効化が「Octalモード指定」で失敗 — CoreS3はQuad接続

sdkconfig.defaults に `CONFIG_SPIRAM_MODE_OCT=y` で有効化したところ、ブートログに:

```
E (465) octal_psram: PSRAM chip is not connected, or wrong PSRAM line mode
E (465) esp_psram: PSRAM enabled but initialization failed. Bailing out.
```

- **原因**: ESP32-S3のPSRAMには Quad(4線)/ Octal(8線)の2方式があり、CoreS3の8MB PSRAMは **Quad接続**。「8MB=N16R8=Octal」という一般的な連想は CoreS3 には当てはまらない(PlatformIOのCoreS3ボード定義も `qio_qspi`)
- **回避策**: `CONFIG_SPIRAM_MODE_QUAD=y` に変更 → `Found 8MB PSRAM device / Speed: 80MHz / memory test OK`
- **予防**: `CONFIG_SPIRAM_IGNORE_NOTFOUND=y` を入れておくと、PSRAM初期化に失敗しても起動は続行する(いきなり文鎮化状態にならない)
- **教訓**: sdkconfig変更はブートログで結果を必ず確認する。エラーが出ていても一見動いてしまうことがある

## 2026-07-20: 「ChatGPT課金」と「API課金」は別物 — 429 insufficient_quota

WorkerからOpenAI APIを叩いたら即エラー:

```
429 {"error": {"message": "You exceeded your current quota, please check your plan and billing details.",
"type": "insufficient_quota", ...
```

- **原因**: ChatGPT(チャット画面)の契約とAPIの支払いは完全に別。ChatGPT Plusに入っていてもAPIクレジットは0
- **回避策**: platform.openai.com の Billing でクレジットをチャージ(最低$5)。チャージ済みでも1年で失効する点に注意

## 2026-07-20: esp版rustcのコンパイラバグ — LLVM ICEと「静かなミスコンパイル」

音声往復コード(TLS通信+PCM処理)を追加したところ、コンパイラ自体が異常終了:

```
rustc-LLVM ERROR: Cannot select: 0x10d2090e0: i32 = Constant<19200>
```

さらに厄介な展開:

1. 定数の書き方を変える → コンパイル時定数畳み込みで同じ値になり**同じICE**
2. `std::hint::black_box` で畳み込み阻止 → コンパイルは通るが、**実行される前のコードのはずのI2C初期化で起動クラッシュ**(LoadProhibited)。クリーンビルドでも再現=ミスコンパイルの疑い
3. `opt-level = "z"` → `"s"` に変更 → クラッシュは消えるが、**画面点灯前にハングする別の壊れ方**(エラーも出ないため、当初ハード故障と誤認して大混乱)

- **切り分けの決め手**: `git stash` で「コミット済みの動いていたコード」に戻してビルド→正常起動。これで「コード起因(=ツールチェーンがこのコードを正しくコンパイルできない)」と確定
- **教訓その1**: コンパイラのICEが出たら、回避してコンパイルを通すのは危険。**ICEは氷山の一角で、水面下に静かなミスコンパイルが隠れていることがある**
- **教訓その2**: 「実行されないはずのコードを追加したら起動しなくなった」はミスコンパイル・リンカ配置問題を疑う
- **教訓その3**: 困ったら最後に動いた状態に戻す退路(gitのコミット粒度)が命綱になる
- **未解決**: ツールチェーン更新(espup update)で解消するか検証予定。WIPコードは `wip/voice-loop` ブランチに保全

### 後日談(2026-07-20午後): 真犯人は自分のコードだった — 誤診の記録

ツールチェーンを1.97.0.0に更新するとICEは消えたが、**起動クラッシュはそのまま再発**。ここで「3種類のコンパイラ設定すべてで同じ場所に落ちる=コンパイラのせいではなく、コードに本物のバグがある」とようやく気づいた。

- **真因**: **メインタスクのスタックオーバーフロー**。新設した `talk()` の4KBローカルバッファ(`chunk: [u8; 4096]`)がインライン化されてmainのスタックフレームが膨張し、上限8192バイトを突破。あふれた書き込みがESP-IDFの割り込み管理領域を壊し、次の深い呼び出し(I2C初期化)でLoadProhibitedクラッシュ
- **修正**: `CONFIG_ESP_MAIN_TASK_STACK_SIZE=20000`(sdkconfig.defaults)で即解決
- **皮肉**: テンプレートのsdkconfig.defaultsには最初から「Rustは大きなスタック変数を確保するなら増やせ」というコメントが書いてあった
- **教訓その1**: 「すべての環境で同じように壊れる」はコンパイラバグではなく自分のバグの証拠。ICE(実在した別のバグ)に引きずられて誤診した
- **教訓その2**: 「実行される前のコードで落ちる」タイプのメモリ破壊は、スタックオーバーフローを最初の容疑者にする
- **教訓その3**: 誤診の過程も記録する。仮説→検証→棄却の流れ自体が財産

## 2026-07-20: 応答再生中に不安定にパニック再起動 — I2Sの「タイムアウト=満杯」はエラーではない

音声往復が動き出した後、「返ってくる時もあるし、再起動する時もある」という不安定さが残った。シリアルで捕捉:

```
thread 'main' (1) panicked at src/main.rs:465:55:
I2S書き込みに失敗: ESP_ERR_TIMEOUT (error code 263)
```

- **原因**: 非ブロッキング再生(`i2s.write(..., タイムアウト0)`)は、DMAバッファに**全く空きがない周回でESP_ERR_TIMEOUTを返す**。これは「今回は書けなかった」という正常な状態なのに、`expect()` がパニックにしていた。タイミング依存なので不安定に見えた
- **修正**: ESP_ERR_TIMEOUTだけ握りつぶして次の周回で再試行(esp-idf-halは部分書き込み成功時はOkを返す実装なので、エラー=0バイトの意味。データ欠落もない)
- **教訓**: 組込みAPIの「タイムアウト」はエラーとは限らない。`expect()` を書く前に「そのエラーは異常系か、正常な待ち状態か」を確認する

## 2026-07-20: Xtensaツールチェーンの更新手順(1.95.0.0→1.97.0.0)

`espup update` は「stable」マークの付いたリリースまでしか上げないため、1.95.0.0のまま更新されなかった。

- **回避策**: `espup install --toolchain-version 1.97.0.0` でバージョン明示インストール
- **ロールバック**: `espup install --toolchain-version 1.95.0.0`
- 1.97.0.0 で LLVM ICE「Cannot select: Constant<19200>」は解消。`opt-level = "z"`(テンプレート既定)に戻して問題なし

## 2026-07-20: 「デバイスが文鎮化した?!」からの復旧手順(実録)

ケーブル抜け事故の後、画面が真っ黒のまま反応しなくなりパニックに。実際はハード無事+上記ミスコンパイル版ファームの「画面点灯前ハング」が重なっただけだった。復旧手順として確立したもの:

1. **落ち着く**。`ls /dev/cu.*` にUSBデバイスが見えていればチップは生きている(文鎮化ではない)
2. ソフトリセット: `espflash reset --port /dev/cu.usbmodemXXX`
3. 電源を完全に切る: 電源ボタン6秒長押し → 短押しで再投入
4. **最終手段(確実)**: リセットボタン(CoreS3は側面)を**緑LEDが点くまで長押し** → ダウンロードモード(書き込み専用状態。画面真っ黒+無反応が正常)に入る → `espflash flash <binary> --port ...` で動くビルドを書き込み → `espflash reset`
- **注**: USBケーブルを替えたり挿し直すとポート名が変わる(`usbmodem101`⇔`usbmodem1101`)。接続エラー時はまず `ls /dev/cu.*`

## (事前調査で判明)SDカードは FAT32 必須

CoreS3 で使う SD カードは FAT32 でフォーマットされている必要がある。64GB 以上のカードは標準で exFAT のため、そのままでは認識しない。16GB(FAT32)を使用する。
